rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client-side direct access.
    // All reads and writes must go through Server Admin SDK (Next.js BFF / GitHub Actions Batch).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
