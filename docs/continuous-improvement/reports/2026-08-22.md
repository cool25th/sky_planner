# Sky Planner Atlas Daily Continuous Improvement Report — 2026-08-22

## 1. Run Information

- 검토일: 2026-08-22 04:00 KST (토요일)
- 실행 모드: ANALYZE_ONLY (자동, 04:00 CronCreate)
- 기준 커밋(직전 검토): `eb67cb5` → 현재 커밋: `c7729f0` (+4 커밋 — 커밋 리뷰 대상)
- stopgap(02:24 KST 스케줄): **성공(1m21s) — 4일 연속 자동 성공**, last_batch 2026-08-21T17:24:36Z(약 1.6시간 전)
- collect-fares(03:55 KST 스케줄): skip-성공(7s) — 2일 연속
- 이전 보고서: 2026-08-21
- 신규 절차 첫 적용: §8.6 커밋 리뷰 + §8.7 보조 관점(토 → 테스트 품질)

## 2. Executive Summary

- **오늘 가장 큰 변화**: 없음(안정일). stopgap 4일 연속, readiness 37/45 유지, 회귀 없음.
- **커밋 리뷰·테스트 품질 렌즈에서 신규 3건 도출** — 모두 "방어 없는 회귀 지점" 계열: 번들 목록 동기화 가드 부재, `lib/data-source`·`lib/secret-validation` 무전용 테스트.
- **이전 개선 검증**: INT-20260821-001 배포 안정(37/45 유지), skip 가드·스톱갑 연속 성공.
- **Go/No-Go**: **NO-GO** (37/45 — 잔여 8 실패는 전부 외부 값: partner 키 4·웹훅 2·이메일 1·SERVICE_REQUIRE_POSTGRES 1)

## 3. Daily Diff

| 구분 | 항목 | 전일(08-21) | 오늘(08-22) | 영향/근거 |
|---|---|---|---|---|
| 배치 | stopgap 자동 실행 | 3일 연속 | **4일 연속 성공** | run 32507970675, 1m21s |
| 배치 | collect-fares | skip-성공(첫 스케줄) | skip-성공 2일 연속 | run 32515826135, 7s |
| 데이터 | source-health / readiness | ready / 37 | ready / 37 | 잔여 실패 목록 동일 |

### 3.1 커밋 리뷰 (§8.6 — `eb67cb5..c7729f0`, 4커밋 diff 건별)

| 커밋 | 개선 후보(관점) | 파일:줄 | 판정 |
|---|---|---|---|
| `302941f` (번들 포함 24파일) | **목록↔체크 동기화를 강제하는 계약 테스트 없음** — 향후 `artifactContains` 추가 시 next.config 미갱신이면 배포 체크가 조용히 다시 실패(정확히 08-21 문제의 재발 경로) | `next.config.ts:5-27` ↔ `lib/readiness-artifacts.ts`, `lib/service-readiness-runtime.ts:54-77` | 백로그 등록 → TEST-20260822-001 |
| `fbfaa9a` (smoke 스크립트 2개 추가) | 같은 동기화 이슈의 연장 | `next.config.ts:25-26` | TEST-001에 흡수 |
| `0a8737d`, `c7729f0` (문서) | 해당 없음 | — | 해당 없음 |

체크리스트 나머지(중복 재등장✗ 데드코드✗ 성능·번들 영향 미미✗ 후속 실행 가능 항목 없음).

### 3.2 오늘의 보조 관점 (§8.7 — 토: 테스트 품질)

- 확인한 질문: lib 모듈별 전용 계약 테스트 보유 여부(grep 매핑), 과거 회귀 사례 영역의 방어, 신뢰 경계 커버.
- 발견:
  - **`lib/data-source.ts` 전용 테스트 0건** — BFF 데이터 경로의 핵심(mock 폴백·진단·source guard). 과거 calendar SQL 파라미터 버그(08-17)도 이 인접 경로였고 방어가 간접적(검색 계약에 의존) → TEST-20260822-002
  - **`lib/secret-validation.ts` 전용 테스트 0건** — 신뢰 경계(placeholder 거부·16자 최소). 보안 경로는 테스트 예외 영역이 아님 → TEST-20260822-003
  - `lib/runtime-state.ts`(0건), `lib/api-response-policy.ts`·`lib/source-policy.ts`(1건) — 우선순위 낮음, 추후 순환에서 재확인
- 정정: 어제 안내한 "내일(일요일)=운영 관측성"은 오기 — 08-22는 토요일이므로 테스트 품질이 올바른 관점. 운영 관측성은 08-23(일).

## 4. Previous Improvement Verification

| ID | 검증 | 상태 |
|---|---|---|
| INT-20260821-001 (번들 포함) | 배포 안정, 37/45 유지 | RESOLVED 재확인 |
| DATA-20260819-001 (스톱갑) | 4일 연속 자동 성공 | RESOLVED 재확인 |
| INT-20260820-001 (skip 가드) | 2일 연속 스케줄 skip-성공 | RESOLVED 재확인 |
| DATA-20260820-001 (env 패키지) | 사용자 값(SUPPORT_EMAIL·웹훅) 대기 | IN_PROGRESS |
| DATA-20260818-002 (빈 배치 가드) | 실 collector 배치 없음 | VERIFYING |
| DATA-20260818-003 | 변화 없음 | DEFERRED |

## 5. Regression Report — 없음 (242/242·Python OK·build OK)

## 6~10. 영역별 개선안 (오늘의 주제: 테스트 품질)

### TEST-20260822-001: 번들 목록 ↔ 정적 체크 경로 동기화 계약 테스트 (Progressive, NEW, XS)

- 문제: `next.config.ts`의 `READINESS_STATIC_FILES`(24개)와 `artifactContains` 호출 경로는 주석으로만 상호 참조 — 동기화 강제 장치 없음. 어긋나면 배포 readiness 체크가 조용히 재실패(08-21 사태 재발).
- 제안: `tests/readiness-bundle-contract.mjs` — next.config.ts에서 경로 목록을 파싱하고, 두 lib 파일의 `artifactContains("…")` 첫 인자 전체를 포함하는지 assert. 포맷터 가드와 같은 정적 스캔 패턴.
- 완료 기준: 신규 artifactContains 경로를 목록 없이 추가하면 테스트 실패. `npm test` 통과.

### TEST-20260822-002: lib/data-source 전용 계약 테스트 (Progressive, NEW, S)

- 문제: BFF 데이터 경로(폴백 판정·진단·source readiness guard)의 직접 테스트 부재 — 회귀가 간접 경로(검색 계약)에서만 우연히 포착됨.
- 제안: 진단 필드 조합·폴백 전환 조건·suppressMockFallback 분기에 대한 계약 테스트.
- 완료 기준: 핵심 분기 각각에 통과/실패 케이스 존재, `npm test` 통과.

### TEST-20260822-003: lib/secret-validation 테스트 (Progressive, NEW, XS)

- 문제: 신뢰 경계(placeholder·길이·localhost 거부) 규칙이 무방어 — 변경 시 운영 preflight가 첫 방어선이 됨.
- 제안: 규칙별 허용/거부 케이스 표 기반 계약 테스트.
- 완료 기준: 각 거부 규칙에 실패 케이스 존재.

## 11. Maturity Score

| 영역 | 전일 | 오늘 | 근거 |
|---|---:|---:|---|
| 내부 모듈 | 2 | 2 | 테스트 품질 렌즈로 커버리티 갭 3건 정량 확인(242개 계약에도 사각 존재) |
| 기타 | — | — | 변동 없음 (usability 2, modularity 2, data_freshness 2, convenience 3) |

## 12. Prioritized Backlog (상위)

| 순위 | ID | 상태 | Impact | Effort | 비고 |
|---:|---|---|---:|---:|---|
| 1 | TEST-20260822-001 | NEW | 4 | XS | 08-21 사태 재발 방지 — 최우선 |
| 2 | TEST-20260822-002 | NEW | 3 | S | BFF 경로 직접 방어 |
| 3 | TEST-20260822-003 | NEW | 3 | XS | 신뢰 경계 |
| 4 | DATA-20260820-001 | IN_PROGRESS | 3 | S(사용자) | 이메일·웹훅 값 대기 |
| 5 | DATA-20260818-003 | DEFERRED | 5 | S(사용자) | partner 키 확보 시 |

## 13. Today's Top 3

1. **TEST-20260822-001** 동기화 계약 테스트 — 배포 readiness 재실패 방지, XS.
2. **TEST-20260822-002** data-source 계약 테스트 — 회귀 사례 있던 경로 직접 방어, S.
3. **TEST-20260822-003** secret-validation 테스트 — 신뢰 경계, XS.

## 15. Updated State

```json
{
  "review_date": "2026-08-22",
  "deployment_version": "c7729f0-era deploy",
  "latest_successful_batch": "2026-08-22 02:24 KST stopgap (4th auto)",
  "source_health": "ready",
  "service_readiness": "37/45 (잔여 8 = 외부 값)",
  "collect_fares_result": "skip-success 7s (2nd)",
  "new_items": ["TEST-20260822-001", "TEST-20260822-002", "TEST-20260822-003"],
  "resolved_items": [],
  "regressed_items": [],
  "blocked_items": ["DATA-20260818-003 (DEFERRED)"],
  "today_top_3": ["TEST-20260822-001", "TEST-20260822-002", "TEST-20260822-003"],
  "next_verification_targets": ["stopgap 5일차", "Top 3 승인·적용 여부", "사용자 env 값", "내일 보조 관점: 운영 관측성(일)"],
  "maturity": {"usability": 2, "functional_modularity": 2, "data_freshness": 2, "internal_modules": 2, "convenience": 3},
  "go_no_go": "NO-GO"
}
```

## 16. 승인 실행 기록 (2026-08-22, 사용자 "TOP 3 전체 승인")

- **TEST-20260822-001 구현·즉시 효과**: `tests/readiness-bundle-contract.mjs`가 **첫 실행에서 실제 누락 포착** — `scripts/ops-alert-smoke.mjs`가 `ops_alert_gate_available` 체크에 쓰이는데 번들 목록에 없었음(웹훅을 주입해도 해당 체크가 배포에서 계속 실패했을 것). next.config에 추가.
- **TEST-20260822-002 구현**: `tests/data-source-contract.mjs` 4건(데모 진단·라벨 매핑·meta envelope·`SERVICE_REQUIRE_POSTGRES` suppressMockFallback 분기). 서버 모듈 직접 import를 위한 resolve 훅(`tests/helpers/`)을 `npm test`에 부착 — server-only·`@/` alias·확장자 없는 상대 import를 계약 테스트에서 쓸 수 있게 됨(이후 모든 서버 모듈 테스트의 기반).
- **TEST-20260822-003 구현**: `tests/secret-validation-contract.mjs` 케이스 표.
- 검증: `npm test` **250/250 통과**(242→250), `npm run build` 통과. 커밋 `331681f` push 완료. 배포 불필요(테스트 인프라 변경 — 단 next.config 누락 수정 포함이므로 다음 배포에 자동 반영).
