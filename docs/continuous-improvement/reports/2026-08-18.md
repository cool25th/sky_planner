# Sky Planner Atlas Daily Continuous Improvement Report — 2026-08-18

## 1. Run Information

- 검토일: 2026-08-18 09:38 KST
- 실행 모드: ANALYZE_ONLY (수동 첫 실행 — 04:00 자동 실행은 호스트 비활성으로 미발동, runCount 0)
- 기준 커밋: `165eb8f` → 현재 커밋: `101bfbf` (+6 커밋, 병행 세션)
- 배포 버전: UNKNOWN (`https://skyplanner-kappa.vercel.app` 200 응답)
- 데이터 백엔드: Firestore beta
- 최신 성공 배치: 2026-08-17T11:21:30Z (source-health 기준, 3소스 87,696 offers)
- collect-fares 실행 결과: **실패(잡 미시작 — 계정 결제/지출한도)**
- 이전 보고서: 없음(첫 실행)

## 2. Executive Summary

- **가장 큰 변화**: 병행 세션이 error boundary·과거 주간 안내·SEL 라우팅·가격 알림 구독 등 6커밋 추가. 시드 항목 3건이 이미 구현 상태로 확인.
- **가장 중요한 신규 문제**: GitHub Actions 결제 실패로 배치 워크플로 2개가 잡 시작 단계에서 실패. CI 경로가 완전히 죽어 있음(DATA-20260818-001, BLOCKED).
- **가장 중요한 회귀**: 없음(로컬 검증 4종 전부 통과).
- **이전 개선 검증**: 첫 실행이라 해당 없음. 대신 시드 9건에 대한 최초 검증 수행(§4).
- **오늘 권장 Top 3**: §13 참조.
- **Go/No-Go**: **NO-GO** — service-readiness 16/45 통과(29 실패). live collector 증거·credential·정책 게이트 부재.

## 3. Daily Diff

### 코드 변경 (커밋 6건 + 미커밋)

| 커밋/파일 | 변경 내용 | 영향 |
|---|---|---|
| `f1acbb7` | 라우트 error boundary + 과거 주간 안내 | 시드 #3, #6 구현 |
| `ea3a95e` | SEL 출발지 귀속, observed/published 스탬프, 최근 본 목적지 | 데이터 신선도 표시 개선 |
| `2d488d9` | 로딩 스켈레톤, error/404 페이지, 가격 추세 차트, 여행 정보 그리드, 통화 스위처 | UX 전반 |
| `b9ad637` | 가격 하락 알림 구독 + 보딩패스 티켓 공유 모달 | 편의 기능 확대 |
| `a0a0c41` | SEL 검색을 ICN/GMP로 Postgres 경로에서 라우팅 | SEL 검색 후보 실현 |
| `101bfbf` | readiness 페이지 소스별 수집 상태 표시 | 운영 가시성 |
| 미커밋 | `command-palette.tsx`, `destination-compare-modal.tsx` (+layout/destination/globals.css) | 병행 세션 진행 중 — 분석에는 미완 상태로 반영됨 |

### 데이터/인프라 변경

| 항목 | 전일 | 오늘 | 판정 |
|---|---|---|---|
| GH Actions 배치 | — | 2 워크플로 모두 잡 미시작 실패 | 이상(결제) |
| source-health | — | ready, 배치 경과 ~13h | 정상 |
| service-readiness | — | not_ready 29/45 실패 | 기존 미달 지속 |

## 4. Previous Improvement Verification (시드 9건 최초 검증)

| 시드 | 오늘 검증 | 새 상태 | 증거 |
|---|---|---|---|
| data_mode 표시 | 구현 확인 | VERIFYING → UX-20260818-001 | 진단 `data_mode` + `dataModeLabel`(2026-08-17 구현). 배포 화면 확인만 남음 |
| 빈 배치 가드 | 구현 확인 | VERIFYING → DATA-20260818-002 | `succeeded===0` skip + `offers.min(1)` + 테스트(2026-08-17). 실배치 재검증 필요 |
| error boundary | 구현됨 | VERIFYING → UX-20260818-002 | `app/{error,not-found}.tsx`, map/offers/destination별 error.tsx (`f1acbb7`) |
| 포맷터 통합 | 중복 확대 | EXPANDED → MOD-20260818-001 | 신규 3컴포넌트에서 `Intl.NumberFormat("ko-KR"…)` 각자 정의 |
| ISO week 2곳 | 정적 검증 통과 | VERIFYING → UX-20260818-003 | `lib/format.ts` `formatWeekNatural`, UI raw `W${` 미발견 |
| 과거 주간 안내 | 구현됨 | VERIFYING → UX-20260818-004 | `isPastWeek` + `f1acbb7` |
| DB 계정 분리 운영 | 미진행 | NEW → INT-20260818-001 | launch-gate review 2026-08-17 |
| partner credential | 여전히 미주입 | BLOCKED → DATA-20260818-003 | service-readiness `source_credentials_present` 등 실패 |
| README stale | 지속 | EXPANDED → MOD-20260818-002 | fare-board·SVG·한계 문단 |

## 5. Regression Report

| ID | 내용 | 증거 | 우선순위 |
|---|---|---|---|
| 없음 | 로컬 테스트/빌드 전부 통과 | §1 참조 | — |

## 6~10. 영역별 신규 개선안

### DATA-20260818-001: GH Actions 결제 실패로 배치 파이프라인 중단 (Reactive, BLOCKED, P0)

- 관찰: `Collect fares`(03:17 KST), `Daily fare batch`(02:00 KST) 모두 2~4초 만에 실패. annotation: "recent account payments have failed or your spending limit needs to be increased".
- 원인: 코드 밖 — GitHub 계정 결제 수단/지출 한도.
- 조치: 운영자가 Settings → Billing & plans에서 결제 수단 갱신 또는 spending limit 상향. 이후 `workflow_dispatch`로 수동 검증.
- 완료 기준: 두 워크플로가 스케줄 실행에서 잡을 시작하고 preflight를 통과할 것.

### DATA-20260818-002: 빈/부분실패 배치 게시 가드 (Reactive, VERIFYING, P2)

- 관찰: 2026-08-17 구현 확인 — `run-collector-sources.mjs`가 `succeeded === 0`이면 batch_state 기록을 skip하고, 소스 스키마 `offers.min(1)`이 0-offer 배치를 거부하며, 계약 테스트가 추가돼 있음.
- 남은 검증: 다음 실배치(결제 복구 후)에서 가드가 실제로 동작하는지 확인 후 RESOLVED 처리.

### MOD-20260818-001: KRW 포맷터 통합 (Progressive, EXPANDED, P2)

- 관찰: `boarding-pass-modal.tsx:20`, `price-alert-modal.tsx:91,119`, `destination-compare-modal.tsx:108`이 동일 `Intl.NumberFormat` 인스턴스를 각각 생성. `lib/format.ts`가 이미 존재.
- 제안: `lib/format.ts`에 KRW 포맷터 추가(또는 기존 함수 재사용) 후 3컴포넌트 교체.
- 완료 기준: 컴포넌트 내 직접 `Intl.NumberFormat` 호출 0건, 계약 테스트 통과.

### INT-20260818-002: sky_collector 테스트 실행 경로 (Reactive, RESOLVED)

- 루트 실행 시 네임스페이스 패키지 충돌로 3 에러. `cd sky_collector && PYTHONPATH=src …`로 5/5 통과 확인. 루프 문서 §15.1 명령 수정으로 조치 완료.

### 기타 기록

- UX-20260818-005: 병행 세션의 커맨드 팔레트·비교 모달(IN_PROGRESS) — 이 루프는 관찰만, 완료 커밋 후 검증 대상.

## 11. Maturity Score

| 영역 | 전일 | 오늘 | 변경 근거 | 다음 단계 |
|---|---:|---:|---|---|
| 화면 사용성 | — | 2 | 첫 기준선. 핵심 흐름·모바일·키보드 내비게이션 구현 | data_mode 표시, 배포 검증 |
| 기능 모듈화 | — | 2 | repository 추상화 존재, 포맷터 중복 잔존 | MOD-001 |
| 데이터 갱신 | — | 1 | 파이프라인 코드는 갖춤, live feed 미연결·CI 중단 | DATA-001/003 해제 |
| 내부 모듈 | — | 2 | 계약 테스트 241개, 연결 재시도 미보강 | INT-001 |
| 편의 기능 | — | 3 | 최근검색·찜·공유·다크모드·알림구독·통화 스위처 | 알림 실제 발송 경로 |

## 12. Prioritized Backlog (상위 7)

| 순위 | ID | 영역 | 상태 | Impact | Effort | 점수 근거 |
|---:|---|---|---|---:|---:|---|
| 1 | DATA-20260818-001 | 데이터 | BLOCKED | 5 | XS(운영자) | 배치 파이프라인 전체 차단, 조치는 결제 설정뿐 |
| 2 | MOD-20260818-001 | 모듈화 | NEW | 3 | XS | 통합 직후 재발한 중복 + 계약 테스트 방어 강화 |
| 3 | UX-20260818-002 외 4건 | UX | VERIFYING | 3 | XS | 배포 화면 검증 스윕으로 5건 일괄 종결 가능 |
| 4 | INT-20260818-001 | 내부 | NEW | 4 | S | 운영 DB 보안 분리, 게이트 통과 전제 |
| 5 | DATA-20260818-003 | 데이터 | BLOCKED | 5 | S(운영자) | credential 주입은 사용자만 가능 |
| 6 | MOD-20260818-002 | 모듈화 | NEW | 1 | XS | 문서 정합성 |

## 13. Today's Top 3

### Top 1 — DATA-20260818-001: GH Actions 결제 조치
- 선정 이유: 배치 파이프라인이 계정 결제 문제로 완전 중단. 코드 작업 없이 운영자 조치만으로 해제되는 최대 병목.
- 오늘 범위: GitHub Settings → Billing & plans에서 결제 수단/지출한도 수정 후 `Collect fares` 수동 실행(workflow_dispatch)으로 잡 시작 확인.
- 완료 기준: 스케줄 실행이 잡을 시작하고 preflight 단계까지 도달.
- 예상 시간: 10분(결제 확인) + 실행 5분.

### Top 2 — MOD-20260818-001: KRW 포맷터 재중복 해소 + 방어 강화
- 선정 이유: 08-17에 `lib/format.ts`로 통합을 완료했으나, 병행 세션의 신규 컴포넌트 3곳(boarding-pass, price-alert, destination-compare)이 직접 `Intl.NumberFormat`을 다시 정의했다. 통합만 하면 재발하므로 계약 테스트로 방어해야 한다.
- 오늘 범위: 3컴포넌트를 `lib/format.ts` 포맷터로 교체 + `tests/format-contract.mjs`에 "컴포넌트/앱 코드 내 직접 `Intl.NumberFormat` 금지" 검사 추가.
- 완료 기준: 컴포넌트 내 직접 호출 0건, `npm test` 통과.
- 예상 시간: 1시간.

### Top 3 — 배포 화면 검증 스윕 (UX-001/002/003/004 + DATA-002 실배치)
- 선정 이유: data_mode 라벨, error boundary, ISO week 자연어 표기, 과거 주간 안내가 모두 코드상 구현 완료(VERIFYING) 상태로, 배포 화면 확인만으로 4건을 RESOLVED로 종결할 수 있다.
- 오늘 범위: `https://skyplanner-kappa.vercel.app`에서 /, /map, /destination, /offers와 과거 주간 접근 확인. 근거는 스크린샷/응답 캡처.
- 완료 기준: 4건 각각 통과/실패 판정과 근거 기록.
- 예상 시간: 30분.

## 14. Learnings

요약은 `LEARNINGS.md` 2026-08-18 항목 참조. 핵심: source-health(ready)와 service-readiness(not_ready)의 기준 차이 — 배치 최신성만으로 live 데이터로 판단하면 안 됨.

## 15. Updated State

```json
{
  "review_date": "2026-08-18",
  "deployment_version": "UNKNOWN",
  "data_backend": "firestore(beta)",
  "latest_successful_batch": "2026-08-17T11:21:30Z",
  "latest_published_batch": "2026-08-17T11:21:30Z(source-health 기준)",
  "firestore_quota_status": "UNKNOWN(운영 audit 미실행)",
  "collect_fares_result": "failed_before_start(billing)",
  "new_items": ["DATA-20260818-001", "MOD-20260818-001", "INT-20260818-002"],
  "resolved_items": ["INT-20260818-002"],
  "regressed_items": [],
  "blocked_items": ["DATA-20260818-001", "DATA-20260818-003"],
  "verifying_items": ["UX-20260818-001", "UX-20260818-002", "UX-20260818-003", "UX-20260818-004", "DATA-20260818-002"],
  "today_top_3": ["DATA-20260818-001", "MOD-20260818-001", "배포 검증 스윕(UX 4건)"],
  "next_verification_targets": ["UX-20260818-002", "UX-20260818-003", "UX-20260818-004"],
  "maturity": {"usability": 2, "functional_modularity": 2, "data_freshness": 1, "internal_modules": 2, "convenience": 3},
  "go_no_go": "NO-GO"
}
```

## 16. 승인 실행 기록 (2026-08-18 오후, IMPLEMENT_APPROVED)

### Top 1 — DATA-20260818-001 (GH Actions 결제): **RESOLVED (public 전환으로 우회)**

- 1차 시도: billing API는 권한 부족(404), workflow_dispatch(run 32089376195)도 결제 annotation으로 잡 미시작 → 사용자 세션에서만 결제 수정 가능.
- 사용자가 "결제 안하고 진행" 승인 → 전환 전 히스토리 비밀 점검(.env·서비스계정·pem 커밋 이력 없음, credential 패턴은 전부 테스트 fixture 가짜값) 통과 후 **저장소 public 전환**.
- 검증: workflow_dispatch(run 32098098864) **성공** — 잡이 시작되고, secrets 미설정으로 설계된 skip 경로로 정상 종료(4초). 결제 블로커 해제.
- 잔여: 배치가 실제로 수집하려면 `DATA-20260818-003`(secrets 주입) 필요. collect-fares 정기 실행(03:17 KST)은 내일 루프에서 확인.

## 17. 사용자 결정 기록 (2026-08-18 저녁)

- **DATA-20260818-003 → DEFERRED**: 사용자 지시 "자격증명은 일단 했다고 하고 넘어감". 실제 상태와 무관하게 진행하기로 함. 확인 시점 GitHub secrets에는 `VERCEL_REVALIDATE_SECRET`만 존재했으므로 "주입 완료"가 아닌 **보류**로 기록 — service-readiness not_ready는 회귀가 아님. live 수집 개시 시 재개.
- **UX-20260818-006 구현**: destination 페이지에 unknown placeId `notFound()` 가드 추가. `npm test` 242/242, `npm run build` 통과. 배포 후 404 확인 시 RESOLVED.
- **MOD-20260818-002 RESOLVED**: README에서 `/fare-board` 라우트 제거·MapLibre 실사용 문장으로 교체.
- 이 시점 남은 열린 항목: `UX-20260818-005`(병행 세션), `DATA-20260818-002`(실배치 검증), 배포 대기 2건(MOD-001, UX-006), `INT-20260818-001`(DB 계정 분리 운영 적용).

## 18. 커밋·배포 기록 (2026-08-18 저녁 마무리)

- 커밋: `d78454a`(병행 세션 — 커맨드 팔레트·비교 모달 + 당일 모든 변경 동반 커밋), `96c3400`(루프 문서), `302d203`(unknown destination not-found). 전부 push 완료.
- 배포: `vercel deploy --prod` 3회(첫 시도 `--scope` 누락으로 Not authorized → `--scope cools-projects-d471a9e6`로 해결).
- 검증(프로덕션): 홈 200, TYO 200(정상 타이틀), ZZZZ9 → not-found UI 렌더. `npm test` 242/242.
- **UX-006 배포 장애 경위(학습)**: 첫 배포들이 unknown 코드에 여전히 셸 페이지를 렌더한 원인은 (1) 병행 세션이 같은 working tree에서 에디터 버퍼로 내 가드를 일시 덮어쓴 시점에 빌드가 돌아 `.next`·Vercel 빌드가 오염됨, (2) 같은 포트(:3000) 충돌로 내 로컬 검증이 병행 세션 서버 응답을 관찰한 적 있음(EADDRINUSE 확인으로 판명), (3) `notFound()`는 force-dynamic 스트리밍에서 본문은 바꾸지만 HTTP 상태는 200으로 남음(soft-404) — generateMetadata 가드로 메타데이터 오염은 제거, 상태 코드 한계는 코드에 ponytail 주석 기록.
- 예방: 병행 세션이 살아 있을 때는 격리 포트 + `.next` 클린 빌드 + 본문 grep 검증을 사용하고, 배포 직전 `grep`으로 디스크 상태 재확인한다.
- 남은 열린 항목(최종): `UX-20260818-005`(병행 세션), `DATA-20260818-002`(실배치 검증), `DATA-20260818-003`(DEFERRED), `INT-20260818-001`(DB 계정 분리 운영 적용).

### Top 2 — MOD-20260818-001 (KRW 포맷터): **구현 완료, 배포 대기(VERIFYING)**

- `boarding-pass-modal.tsx`, `price-alert-modal.tsx`(2곳), `destination-compare-modal.tsx`의 직접 `Intl.NumberFormat`을 `lib/format.ts` `formatMoney` import로 교체.
- `tests/format-contract.mjs`에 가드 추가: app/·components/에서 `Intl.NumberFormat` 직접 생성 금지(재발 시 테스트 실패).
- 검증: `npm test` **242/242 통과**(신규 가드 포함), `npm run build` 통과.
- 참고: `destination-compare-modal.tsx`는 병행 세션의 커밋 전 파일 — 최소 변경(import 1줄 + 로컬 함수 제거)만 적용했음을 해당 세션에서 인지 필요.

### Top 3 — 배포 화면 검증 스윕: **4건 RESOLVED, 신규 1건 발견**

| 항목 | 근거 (https://skyplanner-kappa.vercel.app) | 결과 |
|---|---|---|
| UX-001 data_mode | 홈/지도 스탬프에 "데모 데이터"·"실시간 데이터" 라벨 렌더 | RESOLVED |
| UX-003 ISO week | /map에 "8월 17일 ~ 23일" 등 자연어 주간 표기 | RESOLVED |
| UX-004 과거 주간 | /map?week=2026-W30 → "지난 주간이라 표시할 특가가 없습니다" + "이번 주간으로 다시 검색" 링크 | RESOLVED |
| UX-002 error boundary | error.tsx 5종 존재·빌드 포함·404 페이지("페이지를 찾을 수 없습니다 (404)" + 홈/지도 CTA) 정상. 런타임 예외 경로는 운영에서 안전 유발 불가 — 미검증 기록 | RESOLVED(조건부) |
| **신규** UX-006 | `/destination/UNKNOWN1`이 200으로 "UNKNOWN1(UNKNOWN1) 항공 특가…" 셸 페이지 렌더. 404 카피는 "잘못된 목적지 코드"를 안내 — unknown placeId `notFound()` 처리 누락으로 판단 | NEW |
