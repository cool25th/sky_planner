# Sky Planner Atlas Daily Continuous Improvement Report — 2026-08-28

## 1. Run Information

- 검토일: 2026-08-28 04:00 KST (금요일)
- 실행 모드: ANALYZE + IMPLEMENT_SAFE(§2.3) — 가드 통과(시작 시 워킹트리 clean), 자율 구현 1건
- 기준 커밋(직전 검토): `273d034` → 현재 커밋: `1fcfbf8` (+7 커밋 — 사용자 승인 작업 3 + 루프 문서/백로그 4)
- 배포 버전: 2026-08-27 오전 배포 유지(skip-link 포함, 사용자 배포 검증 기록 `1fcfbf8`). Vercel CLI 관측 불가 — `--scope cools-projects-d471a9e6`가 "specified scope does not exist" 반환(기존 "Not authorized"와 다른 신규 오류, §9.6 메모 갱신 필요)
- 데이터 백엔드: Firestore(beta) / 운영 read model은 Neon Postgres
- **최신 성공 배치: 2026-08-26T18:57Z (stopgap 9일차) — 어젯밤(08-28 02:00 KST) 미실행, 24.1h 경과**
- source-health: **not_ready** (활성 3소스 전부 stale, eligible 0/2). 사이트는 200 유지·"데모 데이터" 라벨 폴백(UX-20260818-001 가드 실전 2회차 정상 작동)
- collect-fares(03:5x KST): **미실행** (마지막 run 2026-08-26T20:00Z skip-성공)
- CI: 어제 push 3건 전부 성공(1m9s)
- 이전 보고서: 2026-08-27
- 보조 관점(§8.7): 금 → **비용·쿼터**

## 2. Executive Summary

- **가장 큰 신규 문제 — 야간 스케줄 배치 전멸 (DATA-20260828-001)**: daily-batch·collect-fares·warmup(하루 ~36회 예정 중 2회)이 모두 실행되지 않았다. 워크플로 4개 전부 `active` 상태이고 런 자체가 생성되지 않음 — 저장소 코드 문제가 아니라 GitHub Actions 스케줄 best-effort 지연·드랍(문서화된 만성 동작, §3.2 출처). 사용자 액션: `gh workflow run daily-batch.yml` 수동 dispatch로 stopgap 재개(운영 DB 쓰기라 자율 실행 불가).
- **회귀 아님 but 상태 악화**: readiness 39→**36/45** — 스테일 연쇄 3개(fresh_successful_batch·eligible_sources_minimum·source_health_ready). 코드 회귀 없음(잔여 6은 여전히 외부 값: partner 키 4·웹훅 1·SERVICE_REQUIRE_POSTGRES 1).
- **자율 구현(TEST-20260824-002) — 첫 실행에서 잠재 결함 포착**: row-mapper 계약 테스트 6건 작성 중 `offerJoinRowSchema.warning_flags`가 null TEXT[] 행을 거부하는 것을 발견(DDL은 `DEFAULT '{}'`뿐, NOT NULL 아님 → NULL 행이 오면 `/api/offers`·`/api/deals/map` 전체 500 잠재 경로). 스키마를 `nullable()`로 1단어 수정(매퍼는 기존 `Array.isArray` 가드로 null 허용). npm test **255→261**, §15.1 전량 통과 — 커밋 대기.
- **보안 잔여 확인**: npm audit 10개(critical 0, high 1 — postcss ≤8.5.22 breaking) — 어제 1단계 기록과 일치, 2단계는 별도 승인 대기.
- **Go/No-Go**: **NO-GO** (배치 미실행 + 외부 값 6)

## 3. Daily Diff

| 구분 | 항목 | 전일(08-27) | 오늘(08-28) | 영향/근거 |
|---|---|---|---|---|
| 배치 | daily-batch stopgap | 9일 연속 성공 | **미실행** | run 목록 — 마지막 2026-08-26T18:56Z |
| 배치 | collect-fares | skip-성공 | **미실행** | 마지막 2026-08-26T20:00Z |
| 배치 | warmup(30분 주기) | 가동 첫날 | **~36회 중 2회만** (04:58Z·16:22Z, 둘 다 지연 실행) | run 목록 |
| 데이터 | source-health | ready | **not_ready**(24.1h) | 3소스 stale, eligible 0 |
| 화면 | 홈 데이터 라벨 | 실시간 데이터 | **데모 데이터**(폴백, 200·1.0s) | curl 실측 |
| 지표 | readiness | 39/45 | **36/45** | 스테일 연쇄 +3 |
| 코드 | 사용자 커밋 | 6건 | 7건(어제 오전 작업 확정분) | §3.1 |
| 의존성 | npm audit | 10(0 critical) | 10(0 critical) — 유지 | §15.1 기준선 |

### 3.1 커밋 리뷰 (§8.6 — `273d034..1fcfbf8`, 7커밋)

| 커밋 | 개선 후보(관점) | 파일:줄 | 판정 |
|---|---|---|---|
| `42b9267` (503 가드 토큰 이동 반영 — 루프 자율분 커밋) | 재검증: readiness 실패 목록에 public_api_503 없음(배포 반영 ✓), 번들 동기화 가드 통과 | `lib/service-readiness-runtime.ts:73-77` | 해당 없음 |
| `dc59f50` (08-27 루프 문서) | 문서만 | `docs/continuous-improvement/*` | 해당 없음 |
| `104d973` (npm audit fix) | 잔여 10개(0 critical·1 high postcss) 실측 유지 — 2단계는 breaking | `package-lock.json` | SEC-20260827-001 1단계 RESOLVED 유지, 2단계 승인 대기 |
| `16c966b` (warmup.yml) | **스케줄러 단일 의존**: 오늘 미실행 사태로 cron best-effort 의존 노출. cron이 `:03/:33` 분산(고부하 :00 회피 권장 준수)인데도 드랍 — 코드 개선 여지 없음, 외부 백업 트리거만 해법 | `.github/workflows/warmup.yml:10` | DATA-20260828-001로 통합 |
| `9b6fd71` (skip-link) | 래퍼 `div#main`은 `<main>` 랜드마크 아님(중복 없음)·`.site-shell` block 레이아웃이라 깨짐 없음·라이브 렌더 확인 — 소견 없음 | `app/layout.tsx:69,89` | 해당 없음 |
| `ca56b66`, `1fcfbf8` (백로그) | 문서만 | `docs/continuous-improvement/BACKLOG.md` | 해당 없음 |

### 3.2 오늘의 보조 관점 (§8.7 — 금: 비용·쿼터)

확인한 질문: Neon 무료 compute-hours 점유, stopgap 일일 재작성 비용, Vercel 배포·함수 한도, Firestore 사용, GH Actions 무료성.

- **Neon — 무료 한도 100 CU-hours/월/프로젝트, 유휴 5분 후 suspend**([neon.com/pricing](https://neon.com/pricing), [neon.com/docs plans](https://neon.com/docs/introduction/plans), [simplyblock 분석](https://vela.simplyblock.io/articles/neon-serverless-postgres-pricing-2026/)). 산정: warmup 정상 가동 시 36회/일 × 웨이크 ~5분 ≈ 3h/일 compute 활성 → 0.25 CU면 **~22.5 CU-h/월(한도의 23%)**, 2 CU면 ~90 CU-h(90%) — compute 크기에 따라 예산 점유가 크게 달라진다. 유기 트래픽 웨이크는 별도 가산. **실측은 Neon 콘솔에서만 가능(사용자 확인 필요)** → INT-20260828-001 등록. warmup 주석의 "항시 웜업 초과" 판단 이후 처음 수치화.
- stopgap 일일 seed: 1.5분 런 × 0.25 CU ≈ 무시 가능(~0.006 CU-h/일). 쓰기량(~87k offers 재계산)은 무료 스토리지 0.5GB 내 재작성 — 증가 아님.
- Vercel: 배포 1~2회/일(한도 100/일 여유), 홈 응답 1.0s(웜). CLI scope 오류로 배포 시각 직접 확인 불가 — 어제 사용자 배포 검증 기록으로 대체.
- Firestore: beta 경로, publish 정지 상태 — 사용 없음(쿼터 무관).
- GH Actions: public 저장소 무료 — warmup 36회/일×14s도 비용 0.
- GitHub 스케줄 신뢰성(오늘 사태의 원인 근거): [community discussions #201738](https://github.com/orgs/community/discussions/201738)("cron 8-14시간 지연, 하루 통째로 드랍"), [#156282](https://github.com/orgs/community/discussions/156282)(best-effort, :00 고부하 지연), [actions/runner #4468](https://github.com/actions/runner/issues/4468)(평균 4시간+ 드리프트). 08-27/28 특정 인시던트 페이지는 [githubstatus.com/history](https://www.githubstatus.com/history)에 없음 — 플랫폼 만성 동작이 이 저장소 관측(지연 17:16→17:26→17:27→18:56→미실행 누적 패턴)과 일치.

## 4. Previous Improvement Verification

| ID | 검증 | 상태 |
|---|---|---|
| SEC-20260827-001 (1단계) | npm audit 실측 10개(0 critical·1 high) — 기록과 일치 | RESOLVED(1단계) 유지 |
| AX-20260825-001 (skip-link) | 라이브 HTML에 skip-link·`id="main"` 렌더 확인(본 루프 curl) | **RESOLVED** |
| PERF-20260824-001 (warmup) | 스케줄 대부분 미실행(GitHub) — 효과 판정 불가, 스케줄러 회복 후 재측정 | RESOLVED(1단계) 유지·추적 |
| public_api_503 회귀 (`42b9267`) | readiness 실패 목록에 없음 — 배포 반영 확인 | 종결 |
| TEST-20260824-002 (row-mapper) | **자율 구현 완료**(§14) — 커밋 대기 | 자율 구현 완료 — 커밋 대기 |
| DATA-20260818-003 | 변화 없음 | DEFERRED |

## 5. Regression Report

| ID | 내용 | 증거 | 상태 |
|---|---|---|---|
| (코드 회귀 없음) | source-health not_ready·readiness 36은 스케줄러 미실행의 결과 — DATA-20260828-001로 관리 | §1·§3 | 신규 문제(회귀 아님 분류) |

## 6~10. 영역별 개선안

### DATA-20260828-001: 야간 스케줄 배치 미실행 — GitHub Actions best-effort 지연·드랍 (Reactive, NEW→VALIDATED, P0급·BLOCKED(외부))

- 관찰: workflows 4개 전부 active인데 daily-batch·collect-fares 런 미생성, warmup 2/36. 워크플로 정의 무결(`daily-batch.yml` cron `0 17 * * *`, `warmup.yml` `3,33 22-23,0-14 * * *` — 분·자정 분산 이미 준수).
- 원인: GitHub 스케줄러 만성 지연·드랍(§3.2 출처). 저장소 코드 아님.
- 영향: 배치 신선도 24h 초과 → 데모 폴백(라벨로 오인 방지됨). 데이터 경로 무중단이나 "실시간 데이터" 제공 중단.
- 회복(사용자 액션, 운영 DB 쓰기): `gh workflow run daily-batch.yml` → 완료 후 source-health `ready`·사이트 라벨 복귀 확인. GitHub이 지연 실행을 회복하면 자동 재개될 수도 있음(8-14h 지연 사례 문서화됨).
- 구조 완화(별도 승인 — 워크플로/외부 서비스): (a) 관찰 유지, (b) 외부 크론(cron-job.org 등)이 repository_dispatch를 호출해 dispatch 기반으로 전환, (c) INT-20260823-001(배치 직후 사이트 프로브)와 묶어 무인 검증 강화.
- 완료 기준: stopgap 재실행·`ready` 복귀 + 다음 스케줄 주기 자동 실행 확인.

### INT-20260828-001: Neon 무료 CU-hours 예산 점유 산정·모니터링 (Progressive, NEW, P3 — 사용자 확인)

- 산정: §3.2(warmup 정상 가동 시 월 ~22.5~90 CU-hours, 한도 100). CU 크기·실측은 Neon 콘솔 확인 필요.
- 완료 기준: 콘솔 사용량 확인 후 warmup 주기(30분→1시간) 조정 필요 여부 판단. 한도 소진 시 compute가 월 말까지 정지됨(초과 청구 없음 — 비용 위험보다 가용성 위험).

### TEST-20260824-002: read-model zod row-mapper 계약 테스트 (Progressive, **자율 구현 완료 — 커밋 대기**)

- 6건: node-pg 타입 매핑(NUMERIC string·Date·TEXT[]) 수용, 계약 위반 행 거부 3종, 숫자 코어션·폴백(좌표·배치 타임스탬프·캐빈별 origin), offers 가격 폴백 체인·duration 환산, mergeMapDeals 캐빈별 최저가·최신 배치 스탬프.
- **결함 포착**: `textArray`가 null 불허 → nullable로 수정(근거·범위는 §14). readiness-bundle 테스트에 이은 "작성 첫 실행 결함 포착" 2번째 사례.

## 11. Maturity Score

| 영역 | 전일 | 오늘 | 변경 근거 | 다음 단계 |
|---|---|---|---|---|
| usability | 2 | 2 | 변동 없음(폴백 라벨 정상) | — |
| functional_modularity | 3 | 3 | 유지(read-model+zod 기반, 가드 추가) | — |
| data_freshness | 2 | **1** | stopgap마저 미실행 — 무인 신선도 유지 장치가 외부 스케줄러에 단일 의존임이 입증 | DATA-20260828-001 |
| internal_modules | 2 | 2 | 유지(잠재 500 경로 1건 제거 — 커밋 대기) | — |
| convenience | 3 | 3 | 변동 없음 | — |

## 12. Prioritized Backlog (상위 7개)

| 순위 | ID | 영역 | 유형 | Impact | Risk | Confidence | Effort | 점수 | 상태 |
|---:|---|---|---|---:|---:|---:|---:|---:|---|
| 1 | DATA-20260828-001 | 데이터 | Reactive | 5 | 4 | 5 | XS(사용자) | 높음 | BLOCKED(외부) |
| 2 | SEC-20260827-001 (2단계) | 보안 | Reactive | 3 | 2 | 4 | S(승인) | 중 | 승인 대기 |
| 3 | TEST-20260824-002 | 내부 | Progressive | 3 | 1 | 5 | S(완료) | 중 | **자율 구현 완료 — 커밋 대기** |
| 4 | INT-20260823-001 | 내부 | Progressive | 3 | 1 | 4 | XS(승인) | 중 | NEW — DATA-001 완화와 묶음 검토 |
| 5 | DATA-20260820-001 (잔여 웹훅) | 데이터 | Reactive | 2 | 1 | 4 | XS(사용자) | 중 | IN_PROGRESS |
| 6 | INT-20260828-001 | 내부 | Progressive | 2 | 2 | 3 | XS(사용자 확인) | 중하 | NEW |
| 7 | DATA-20260818-003 | 데이터 | Reactive | 5 | 3 | 3 | L | — | DEFERRED(사용자 결정) |

## 13. Today's Top 3

1. **[DATA-20260828-001] stopgap 수동 재개(사용자 액션)** — `gh workflow run daily-batch.yml` dispatch 후 source-health `ready`·"실시간 데이터" 라벨 복귀 확인. 범위: 실행·확인만. 제외: 워크플로 수정. 완료 기준: batch_state 24h 내 복귀. 검증: §9.5 curl. 예상 10분.
2. **[DATA-20260828-001 구조 완화 방향 결정(사용자 결정)]** — (a) 관찰 유지 (b) 외부 크론→repository_dispatch 전환 (c) INT-20260823-001 프로브와 묶어 자동 알림. 결정 후 승인 절차 진행. 예상 결정 15분(구현은 별도).
3. **[TEST-20260824-002 커밋(사용자 액션)]** — `git diff` 검토 후 커밋(테스트 6건 + 스키마 1단어). §15.1 전량 통과 완료.

## 14. 자율 구현 내역 (§2.3)

| 항목 | 수정 파일 | 변경 요약 | §15.1 결과 |
|---|---|---|---|
| TEST-20260824-002 row-mapper 계약 테스트 + 잠재 500 경로 수정 (Reactive 결함 포착 포함) | `tests/row-mappers-contract.mjs` (신규 6 테스트), `lib/read-model/row-mappers.ts:23-27` | zod row 계약 고정(타입 수용·거부·코어션·폴백·merge). 테스트 첫 실행에 `warning_flags: null` 행 거부 발견 → `textArray`를 `.nullable()`로 수정(DDL `TEXT[] DEFAULT '{}'`는 NOT NULL 아님; `offers-query.ts`가 `SELECT o.*`로 COALESCE 없이 전달; 매퍼는 기존 `Array.isArray` 가드로 null 처리 — 스키마만 실측과 불일치) | npm test **261/261**(+6)·lint 0진단·tsc 0오류·Python 4/4·sky_collector 5/5·build OK |

- 자율 구현 #2는 미실행 — Top 3 중 안전 클래스 잔여 항목 없음(배치 재개는 운영 DB 쓰기, SEC 2단계는 breaking 변경 — 모두 승인 대상). **커밋하지 않음(작업 tree 보관, git add·commit·push 없음)**.

## 15. Updated State

```json
{
  "review_date": "2026-08-28",
  "deployment_version": "1fcfbf8-era (08-27 오전 배포 유지 — 사용자 검증 완료)",
  "data_backend": "firestore(beta) / Neon Postgres read model",
  "latest_successful_batch": "2026-08-26T18:57Z stopgap (9th) — 08-28 02:00 KST 미실행, 24.1h 경과",
  "latest_published_batch": "동일",
  "source_health": "not_ready (스케줄러 미실행 — 사이트 데모 폴백 정상)",
  "service_readiness": "36/45 (스테일 연쇄 +3; 외부 값 6 불변)",
  "firestore_quota_status": "사용 없음(publish 정지)",
  "collect_fares_result": "미실행 — 마지막 2026-08-26T20:00Z skip-성공",
  "warmup_result": "~36회 예정 중 2회(지연 실행)",
  "vercel_cli": "scope 오류로 관측 불가(신규 — §9.6 메모 갱신)",
  "new_items": ["DATA-20260828-001", "INT-20260828-001"],
  "resolved_items": [],
  "regressed_items": [],
  "blocked_items": ["DATA-20260828-001 (GitHub 스케줄러 외부 의존)", "DATA-20260818-003 (DEFERRED)"],
  "today_top_3": ["stopgap 수동 재개(사용자)", "스케줄 의존 완화 방향 결정(사용자)", "TEST-20260824-002 커밋"],
  "next_verification_targets": ["dispatch 후 ready 복귀·사이트 라벨", "다음 스케줄 주기 자동 실행 여부", "Neon 콘솔 CU-hours 실측", "내일 보조 관점: 테스트 품질(토)"],
  "maturity": {"usability": 2, "functional_modularity": 3, "data_freshness": 1, "internal_modules": 2, "convenience": 3},
  "go_no_go": "NO-GO"
}
```

## 16. 승인 실행 말록 (2026-08-28 오전 — 사용자 "전체 승인")

| 항목 | 실행 | 결과 |
|---|---|---|
| Top 3 #1 stopgap 재개 | `gh workflow run daily-batch.yml` dispatch | **성공** — run 33134401615 (1m25s). 직전에 스케줄러가 ~8h15m 지연 자체 회복(10:14 KST schedule run 성공)해 이중 재게시. source-health **`ready`**(eligible 3/2, blockers 0) 실측 |
| Top 3 #2 구조 완화 | `daily-batch.yml`에 `repository_dispatch` 트리거(외부 크론용 curl 예시 주석) + 배치 직후 `/api/ops/source-health` 프로브 스텝 추가(INT-20260823-001) | 구현·커밋 — push·배포 대기. 새 의존성 없음(curl+grep) |
| Top 3 #3 커밋 | TEST-20260824-002(테스트 6건+스키마 수정) / 워크플로 / 루프 문서 3커밋 | pathspec 커밋 완료 — **push는 사용자 실행** |

**실행 중 신규 발견 (UX-20260828-001, P2)**: ready 복귀 후에도 홈이 "데모 데이터". 재현: W35(현재 주)=demo vs W36=live. 원인: `resolveMapDataFromPostgres`가 과거출발일 필터(`best_depart_date >= CURRENT_DATE`) 후 0행이면 null → "DB 미구성"과 동일한 mock 폴백. 주 후반(금~일) 현재 주간은 항상 이 경로(홈 기본 week가 `meta.weeks[0]`=현재 주). 방향 결정 대기: (a) honest empty 전환 (b) 데모 미리보기 유지 — data_mode 의미 변경이라 계약 테스트 동반 필요.

상태 갱신: DATA-20260828-001 → **RESOLVED(즉시 회복)·추적**(종결 조건: 내일 02:00 정시 실행+프로브 첫 동작) / INT-20260823-001 → IMPLEMENTED(배포 대기) / TEST-20260824-002 → 커밋 완료(push 대기) / 신규 UX-20260828-001 등록.

## 17. 후속 승인 실행 (같은 날 오후 — "푸시 해줘" + "a 로 적용")

| 항목 | 실행 | 결과 |
|---|---|---|
| 3커밋 push | `git push` (1fcfbf8..9a76bd7) | 완료 — CI·배포 트리거 |
| UX-20260828-001 옵션 (a) | `lib/read-model/map-query.ts`: 0행 조기 null → `emptyMapDataForQuery`(빈 live `MapData`, deals[]·summary 0) 반환. `app/page.tsx`: 홈 featuredDeals 0건 시 빈 상태("예약 가능한 특가가 없습니다"+다음 주간 링크, `map-empty-state` 클래스 재사용). 맵은 기존 MapSplitView 빈 상태가 그대로 발화. 계약 테스트 1건 추가 | §15.1 전량 통과 — npm test **262/262**·lint·tsc·build·Python. 커밋·push 후 배포 실측 대기 |

- 검증 계획: 배포 후 금~일 홈/맵 SSR이 `"실시간 데이터"` 라벨 + 빈 상태 안내로 응답하는지 curl 확인(데모 폴백 소멸). 과거 주간(UX-004 안내)도 이제 demo 가격 대신 빈 live로 일관.
- **배포 실측 종결(같은 날)**: W35 API `live`·deals 0 / W36 `live`·11건, 홈 SSR "실시간 데이터" 스탬프 + "예약 가능한 특가가 없습니다"·"다음 주간으로 검색" 렌더(잔여 "데모 데이터" 문자열은 정적 beta 안내 문구). readiness **39/45** 복귀(스테일 연쇄 해소, 잔여 6=외부 값). UX-20260828-001 → RESOLVED.
- 잔여 동일 패턴(관찰 후 결정): `/api/deals/calendar`·`/api/offers`도 0행 시 mock 폴백 — 직접 URL 과거일 진입 시에만 데모 표시(라벨은 정확). 홈·맵 기본 경로는 본 수정으로 해결.
