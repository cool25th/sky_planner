# Sky Planner Atlas Daily Continuous Improvement Report — 2026-08-19

## 1. Run Information

- 검토일: 2026-08-19 04:00 KST
- 실행 모드: ANALYZE_ONLY (자동, 04:00 CronCreate)
- 기준 커밋: `1f96ac4` → 현재 커밋: `1f96ac4` (신규 커밋 없음, working tree 깨끗함)
- 배포 버전: 어제 저녁 배포 그대로 (UX-006 가드 포함)
- 데이터 백엔드: Firestore beta
- 최신 성공 배치: 2026-08-17T11:21:30Z — **약 32시간 경과 (SOURCE_MAX_STALE_HOURS=24 초과)**
- collect-fares(03:55 KST 스케줄): **잡 시작·실행됨(결제 해제 최종 확인), 1m1s 후 실패** — 실패 단계: service launch audit(env 없음) + collector artifacts 업로드(수집 artifact 없음). `Publish Daily Firestore Batch` 단계는 성공.
- daily-batch(02:21 KST): 성공(8s, secrets 미설정 skip-by-design)
- 이전 보고서: 2026-08-18

## 2. Executive Summary

- **가장 큰 변화**: 배포 데이터가 24시간 한도를 넘어 source-health가 `ready → not_ready`로 전환. 활성 3소스 전부 `stale` 차단, search_eligible 0/2. 사이트는 200 유지하며 **"데모 데이터" 라벨 폴백으로 무결** — UX-20260818-001(data_mode 표시)이 실전 위기에서 제역할을 함.
- **가장 중요한 신규 문제**: CI에서 Firestore publish가 성공했는데도 최신성 지표(source-health last_batch)가 그대로 — **이중 백엔드 신선도 판정 불일치** 의심(INT-20260819-001).
- **회귀**: 없음. 로컬 npm test 242/242, backend 4/4, sky_collector 5/5, build 통과. 배포 페이지(홈/맵/오퍼) 200.
- **이전 개선 검증**: DATA-001 스케줄 실행으로 최종 확인. UX-005(커맨드 팔레트) ⌘K 라이브 확인 → RESOLVED. DATA-002는 실배치 없어 유지.
- **오늘 권장 Top 3**: §13.
- **Go/No-Go**: **NO-GO** — service-readiness 13/45 통과(전일 16 → 32 실패, 스테일 연쇄).

## 3. Daily Diff

### 코드 변경 — 없음 (신규 커밋 0, working tree 깨끗)

### 데이터/인프라 변경

| 항목 | 전일(08-18) | 오늘(08-19) | 판정 |
|---|---|---|---|
| source-health | ready (배치 13h 경과, eligible 3) | **not_ready** (32h 경과, eligible 0, `insufficient_search_eligible_sources`) | 악화 — DATA-003 DEFERRED의 직접 결과 |
| service-readiness | 16/45 통과 | **13/45 통과** (fresh_successful_batch, source_health_ready 등 신규 실패) | 악화(연쇄) |
| 배포 페이지 | 실시간/데모 혼합 표시 | 전부 200 + "데모 데이터" 폴백 일관 | 정상 동작(설계대로) |
| collect-fares 스케줄 | 결제로 잡 미시작 | **잡 시작·단계 실행 확인**, audit 단계 실패 | DATA-001 해제 최종 확인 |

## 4. Previous Improvement Verification

| ID | 이전 상태 | 오늘 검증 | 새 상태 | 증거 |
|---|---|---|---|---|
| DATA-20260818-001 | RESOLVED | 스케줄 실행도 잡 시작 | RESOLVED (재확인) | run 32173705484, 1m1s 실행 |
| UX-20260818-005 | IN_PROGRESS(병행) | `d78454a` 커밋 + 배치 + ⌘K 라이브 | **RESOLVED** | 홈 ⌘K 마크업 확인 |
| UX-20260818-001 | RESOLVED | 실전 스테일 상황에서 데모 라벨 정상 | RESOLVED (효과 재확인) | 홈/맵/오퍼 "데모 데이터" ×6 |
| UX-20260818-006 | RESOLVED | 회귀 없음 | RESOLVED 유지 | — |
| DATA-20260818-002 | VERIFYING | 실배치 없음 | VERIFYING 유지 (DATA-003와 엮임) | 배치 미발생 |
| DATA-20260818-003 | DEFERRED | 결과 관찰: 24h 초과→not_ready→데모 폴백 | DEFERRED 유지 (관찰 기록 EXPANDED) | §3 표 |

## 5. Regression Report — 없음

## 6~10. 영역별 개선안

### INT-20260819-001: 신선도 판정 소스 불일치 조사 (Reactive, NEW, P1)

- 관찰: collect-fares의 `Publish Daily Firestore Batch`(quota guard) 단계가 성공(2026-08-18T18:56Z)했는데도 source-health의 `last_batch`는 2026-08-17T11:21Z 그대로.eligible 0, 전소스 stale.
- 가설: source-health는 Postgres `batch_state`/`source_health`를 보고, Firestore publish는 Firestore `service_state`만 갱신 — 어느 쪽이 진실인지 판정 소스가 갈라져 있음(상세는 UNKNOWN, 배포 env 구성 확인 필요).
- 완료 기준: 어느 저장소가 최신성의 단일 기준인지 정의(DECISIONS 등록)하고, publish 경로가 그 기준을 갱신하도록 일치. Firestore가 기준이면 source-health가 Firestore를 읽도록 수정(승인 후).

### DATA-20260819-001: secrets 주입 전 beta 신선도 스톱갭 부재 (Reactive, NEW, P2)

- 관찰: DATA-003 DEFERRED 상태에서 live 배치가 없으므로 매일 24시간 경과 후 not_ready→데모 폴백이 반복됨. 사용자 무결하나 "실시간" 경험은 사실상 종료 상태.
- 선택지: (a) DATA-003 재개(근본 해결) (b) 임시 재게시 자동화(정기적 mock publish로 batch_state 갱신 — 데모지만 fresh 상태 유지) (c) 현상 유지.
- 완료 기준: 세 선택지 중 하나를 사용자가 결정하고 DECISIONS에 기록.

### 기타

- INT-20260818-001(DB 계정 분리 운영 적용): 계속 OPEN(운영 작업).
- UX-20260818-006 soft-404 한계(hard 404 필요 시 middleware): 변동 없음.

## 11. Maturity Score

| 영역 | 전일 | 오늘 | 변경 근거 | 다음 단계 |
|---|---:|---:|---|---|
| 화면 사용성 | 2 | 2 | 스테일 위기에서 데모 라벨 폴백 정상 작동(방어 확인) | — |
| 기능 모듈화 | 2 | 2 | 변동 없음 | — |
| 데이터 갱신 | 1 | 1 | 24h 초과 악화 관찰 — 배치 무실행(DATA-003 연쇄). 구조는 그대로 | DATA-003 재개 또는 스톱갭 |
| 내부 모듈 | 2 | 2 | 판정 소스 불일치 신규 발견(INT-20260819-001) | 조사 |
| 편의 기능 | 3 | 3 | 커맨드 팔레트·비교 모달 정식 종결 | — |

## 12. Prioritized Backlog (상위)

| 순위 | ID | 영역 | 상태 | Impact | Effort | 비고 |
|---:|---|---|---|---:|---:|---|
| 1 | DATA-20260818-003 | 데이터 | DEFERRED | 5 | S(운영자) | 재개 결정이 신선도·readiness의 근본 해제 |
| 2 | INT-20260819-001 | 내부 | NEW | 4 | M | 판정 소스 단일화 — 스톱갭 설계의 전제 |
| 3 | DATA-20260819-001 | 데이터 | NEW | 3 | S | 스톱갭 결정(재개/임시재게시/유지) |
| 4 | INT-20260818-001 | 내부 | NEW | 4 | S(운영자) | DB 계정 분리 운영 적용 |
| 5 | DATA-20260818-002 | 데이터 | VERIFYING | 3 | — | 실배치 대기 |

## 13. Today's Top 3

1. **DATA-20260818-001이 아닌 DATA-20260818-003 재개 결정** — 선정 이유: 24h 스테일→데모 폴백 반복이 DATA-003(자격증명) 보류의 직접 결과. 재개하면 collect-fares가 매일 성공하고 신선도·readiness가 회복됨. 사용자 결정만으로 해제.
2. **INT-20260819-001 신선도 판정 소스 조사** — 선정 이유: Firestore publish 성공과 Postgres batch_state 불일치는 스톱갭·재개 어느 쪽이든 먼저 풀어야 할 전제. 코드·워크플로 조사(ANALYZE로 범위 확정 → 승인 후 수정).
3. **DATA-20260819-001 스톱갭 결정** — 선정 이유: 재개를 미룬다면 매일 데모 폴백이 지속됨. 임시 재게시 자동화 여부를 결정해 예측 가능한 상태로 만들어야 함.

## 14. Learnings

- data_mode 라벨링(UX-001)이 실전 스테일 상황에서 사용자에게 정직하게 작동함 — "데모 데이터" 표시로 오인 없이 폴백.
- collect-fares는 secrets 없이도 잡 시작~Firestore publish까지 실행됨: 결제 해제가 스케줄 실행에서도 유효(데이터 없이 publish만 성공하는 워크플로 구조 확인).
- 상세는 LEARNINGS.md 2026-08-19 항목.

## 15. Updated State

```json
{
  "review_date": "2026-08-19",
  "deployment_version": "1f96ac4-era deploy (2026-08-18 evening)",
  "data_backend": "firestore(beta)",
  "latest_successful_batch": "2026-08-17T11:21:30Z (32h stale)",
  "source_health": "not_ready (0 eligible, all stale)",
  "service_readiness": "13/45",
  "collect_fares_result": "job_started_then_failed_at_audit (no secrets)",
  "new_items": ["INT-20260819-001", "DATA-20260819-001"],
  "resolved_items": ["UX-20260818-005"],
  "regressed_items": [],
  "blocked_items": ["DATA-20260818-003 (DEFERRED by user)"],
  "today_top_3": ["DATA-20260818-003 재개 결정", "INT-20260819-001", "DATA-20260819-001"],
  "next_verification_targets": ["collect-fares 내일 03:5x KST 실행", "DATA-20260818-002 실배치", "INT-20260819-001 판정 소스"],
  "maturity": {"usability": 2, "functional_modularity": 2, "data_freshness": 1, "internal_modules": 2, "convenience": 3},
  "go_no_go": "NO-GO"
}
```

## 16. 후속 실행 (2026-08-19, 사용자 스톱갑 승인)

- **DATA-20260819-001 스톱갑 구현**: 사용자가 선택지 (b) 임시 재게시를 승인. `daily-batch.yml`에 stopgap 스텝 추가 — collector secrets(READY)가 없는 동안 `npm run db:seed`가 Postgres `batch_state`를 재게시해 24h 신선도를 유지한다(ADR-005). READY가 true가 되면(DATA-003 재개) 자동 비활성.
- **INT-20260819-001 종결(원인 판명)**: source-health는 Postgres `batch_state` 직접 쿼리(코드 확인). 어제의 "Firestore publish 성공"은 env 부재 시 no-op 성공 — 불일치가 아니라 무효 성공이었다.
- **남은 BLOCKER(사용자 액션 1건)**: GitHub secrets에 실제 `DATABASE_URL` 필요. Vercel에서 Sensitive 변수로 지정돼 있어 `vercel env pull`로 값 복사가 불가(시도 후 확인, 임시값 시크릿은 즉시 삭제). Vercel 대시보드의 값을 사용자가 `gh secret set DATABASE_URL`로 주입해야 스톱갑이 작동한다. 주입 전까지 워크플로는 기존 동작(skip)을 유지하므로 안전.
- 검증 계획: 시크릿 주입 후 `gh workflow run daily-batch.yml` → seed 성공·source-health ready 복귀·data_mode 전환 확인. 02:00 KST 이전 주입 시 다음 날 4시 루프가 자동 확인.

### 가동 완료 (같은 날)

- 시크릿 주입 경로 확정: Vercel Sensitive 복사 불가 → `.env.local`의 동일 Neon 운영 URL(`postgresql://…aws.neon.tech`, 122자)을 사용. **첫 주입에서 값이 큰따옴표로 감싸진 채 들어가 파서가 호스트를 `base`로 오인해 실패**(run 32191507462) — 따옴표 제거 후 재주입으로 해결.
- 워크플로 문법 수정: `if:`에서 `secrets` 컨텍스트 사용 불가 → job `env.SEED_DB`로 우회(커밋 `ddb8386`).
- **최종 검증(2026-08-19 07:15 KST)**: run 32191632315 성공(1m6s) → `/api/ops/source-health` **ready**(eligible 3, blocker 0, source_flags 복원), 홈 "실시간 데이터" 라벨 복원. 매일 02:00 KST 자동 재게시 확립.
- 운영 DB가 **Neon**(ap-southeast-1)으로 확인됨 — 루프 문서 §1의 "Neon 아님" 기술은 데이터 제공자 관점에서 수정 필요(§1.2 각주).

## 17. INT-20260818-001 계정 분리 운영 적용 (2026-08-19, 사용자 "다음 작업 진행" 승인)

- **1단계 적용(ADR-006)**: 운영 Neon(`neondb`)에 `sky_planner_read`/`sky_planner_ingest`/`sky_planner_migration` 생성(무작위 hex 비밀번호) + 가산적 GRANT만(11개 테이블 중 read=전체 SELECT, ingest=7개 핵심 테이블 RW) + default privileges. 소유권 이전·PUBLIC 회수는 보류.
- **프로브 검증**: read INSERT/CREATE 차단(42501)✓, ingest INSERT 허용·CREATE 차단✓ — `smoke:prod-readiness` db_roles 게이트의 실질 요건 충족.
- **소비자 전환**: GitHub `DATABASE_URL`·`DATABASE_INGEST_URL` → ingest URL(stopgap seed 전용), Vercel `DATABASE_READ_URL` 추가(Sensitive, BFF 조회 전용) 후 재배포. 검증: source-health ready 유지, 사이트 "실시간 데이터" 유지, stopgap 재실행(32192313893, ingest 역할) 성공.
- **자격증명 보관**: `.env.local`(gitignore)에 3역할 URL 기록. 임시 비밀번호 파일은 shred 완료.
- 효과: BFF 앱 계정이 읽기 전용으로 축소(앱 침해 시 데이터 오염 경로 차단), CI seed 계정은 제한 쓰기만. 남은 노출: `.env.local`과 Vercel `DATABASE_URL`(owner) — 2단계에서 축소.
